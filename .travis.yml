---
language: php
php: 5.6

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq python-apt python-pycurl
  - sudo apt-get remove --purge mysql-server
install:
  - sudo pip install ansible
before_script:
  - ansible-playbook -i ansible/inventory/travis ansible/provision.yml
script:
  - mysql --version
  - mysql -u root --password=12345 -e "select host, user, password from mysql.user;"
  - mysql -u root -e "select host, user, password from mysql.user;"
  - mysql -u api --password=bocoup -e "select * from api;"
  - cd test
  - phpunit --debug
